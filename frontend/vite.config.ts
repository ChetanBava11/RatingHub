// Standard Vite + TanStack Start configuration.
// Replaces the previous platform-specific vite config wrapper.
// Plugins included here provide the same functionality as before:
//   tailwindcss, vite-tsconfig-paths, tanstackStart, @vitejs/plugin-react
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import tsConfigPaths from "vite-tsconfig-paths";
import { tanstackStart } from "@tanstack/react-start/plugin/vite";

export default defineConfig({
  plugins: [
    tailwindcss(),
    tsConfigPaths({ projects: ["./tsconfig.json"] }),
    tanstackStart({
      // Redirect TanStack Start's bundled server entry to src/server.ts (our SSR error wrapper).
      server: { entry: "server" },
    }),
    react(),
  ],
});
